<UserControl x:Class="GitGUI.Controls.DiffViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GitGUI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- OLD Version -->
        <StackPanel Grid.Column="0" Margin="5">
            <TextBlock Text="Before" FontWeight="Bold" Margin="0,0,0,5"/>
            <ListBox x:Name="OldLines"/>
        </StackPanel>

        <!-- Separator -->
        <Border Grid.Column="1" Width="1" Background="Gray"/>

        <!-- NEW Version -->
        <StackPanel Grid.Column="2" Margin="5">
            <TextBlock Text="After" FontWeight="Bold" Margin="0,0,0,5"/>
            <ListBox x:Name="NewLines"/>
        </StackPanel>
    </Grid>
</UserControl>
