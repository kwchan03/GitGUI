<Page x:Class="GitGUI.Pages.OperationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GitGUI.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Operation">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="150"/>
        </Grid.RowDefinitions>

        <!-- 🔹 Repo Info -->
        <DockPanel Grid.Row="0" LastChildFill="True" VerticalAlignment="Top" Grid.ColumnSpan="2">
            <Label Content="Repository:" FontWeight="Bold" VerticalAlignment="Center" DockPanel.Dock="Left"/>
            <Button Content="Create" MinWidth="75" Margin="5,0,0,0" VerticalAlignment="Center" DockPanel.Dock="Right" Click="Button_Click"/>
            <Button Content="Clone" MinWidth="75" VerticalAlignment="Center" DockPanel.Dock="Right" Click="Button_Click"/>
            <TextBox x:Name="RepoPathTextBox" Margin="5,0" VerticalAlignment="Center"
     HorizontalAlignment="Stretch" />
        </DockPanel>

        <!-- 🔸 Main Content -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2.5*"/>
            </Grid.ColumnDefinitions>

            <!-- 📊 Commit Graph Panel -->
            <GroupBox Header="Commit Graph" Grid.Column="0" Margin="5">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="CommitGraphList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="5">
                                    <Ellipse Width="10" Height="10" Fill="DarkBlue" Margin="5"/>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Message}" FontWeight="Bold"/>
                                        <TextBlock Text="{Binding Hash}" FontSize="10" Foreground="Gray"/>
                                        <TextBlock Text="{Binding Date}" FontSize="10" Foreground="DarkGray"/>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </GroupBox>

            <!-- 📁 File Changes Panel -->
            <GroupBox Header="Changes" Grid.Column="1" Margin="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <ListBox x:Name="ChangesListBox" Grid.Row="0"/>

                    <WrapPanel Grid.Row="1" Margin="5" HorizontalAlignment="Center">
                        <Button Content="Stage" Width="75" Margin="5"/>
                        <Button Content="Unstage" Width="75" Margin="5"/>
                        <Button Content="View Diff" Width="75" Margin="5"/>
                    </WrapPanel>
                </Grid>
            </GroupBox>

            <!-- 📝 Commit & Branch Panel -->
            <GroupBox Header="Commit &amp; Branch" Grid.Column="2" Margin="5">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        <TextBlock Text="Commit Message:"/>
                        <TextBox x:Name="CommitMessageBox" AcceptsReturn="True" Height="60" Margin="0,5"/>
                        <Button Content="Commit" Width="100" Margin="0,5,0,10"/>

                        <TextBlock Text="Branch:"/>
                        <ComboBox x:Name="BranchComboBox" Margin="0,5"/>
                        <WrapPanel Margin="0" HorizontalAlignment="Center">
                            <Button Content="Checkout" Width="100" Margin="5"/>
                            <Button Content="New Branch" Width="100" Margin="5"/>
                        </WrapPanel>

                        <WrapPanel HorizontalAlignment="Center">
                            <Button Content="Push" Width="100" Margin="5"/>
                            <Button Content="Pull" Width="100" Margin="5"/>
                        </WrapPanel>
                    </StackPanel>
                </ScrollViewer>
            </GroupBox>
        </Grid>

        <!-- 🔻 Output Log -->
        <GroupBox Header="Output Log" Grid.Row="2" Grid.ColumnSpan="2">
            <TextBox x:Name="OutputTextBox"
             IsReadOnly="True"
             TextWrapping="Wrap"
             VerticalScrollBarVisibility="Auto"
             HorizontalScrollBarVisibility="Auto"
             FontFamily="Consolas"
             Background="Black"
             Foreground="LightGreen"/>
        </GroupBox>
    </Grid>
</Page>
