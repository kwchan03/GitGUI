<Page x:Class="GitGUI.Pages.PullRequestReview"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GitGUI.Pages"
      xmlns:controls="clr-namespace:GitGUI.Controls"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PullRequestReview">

    <Grid Background="White" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="Auto"/> <!-- Summary -->
            <RowDefinition Height="*"/>   <!-- Main Content -->
        </Grid.RowDefinitions>

        <!-- 🔹 PR Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,10">
            <TextBlock Text="Pull Request:" FontWeight="Bold" FontSize="16" Margin="0,0,10,0"/>
            <TextBlock x:Name="PRTitle" Text="Fix login bug" FontSize="16"/>
            <TextBlock Text="by" Margin="10,0,0,0"/>
            <TextBlock x:Name="PRAuthor" Text="octocat" FontStyle="Italic" Foreground="Gray"/>
            <WrapPanel HorizontalAlignment="Right" Margin="20,0,0,0">
                <Button Content="Approve" Margin="5"/>
                <Button Content="Comment" Margin="5"/>
                <Button Content="Request Changes" Margin="5"/>
            </WrapPanel>
        </StackPanel>

        <!-- 📝 PR Summary -->
        <Border Grid.Row="1" BorderBrush="LightGray" BorderThickness="1" Padding="10" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="Description:" FontWeight="Bold"/>
                <TextBlock x:Name="PRDescription" Text="This PR fixes issue #42 by..." TextWrapping="Wrap"/>
                <TextBlock Text="Created: 2024-06-01" FontSize="10" Foreground="Gray"/>
            </StackPanel>
        </Border>

        <!-- 📂 Files & Diff Viewer -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/> <!-- File list -->
                <ColumnDefinition Width="5*"/> <!-- Diff viewer -->
            </Grid.ColumnDefinitions>

            <!-- 📁 File List -->
            <GroupBox Header="Changed Files" Grid.Column="0" Margin="5">
                <ListBox x:Name="FileListBox" SelectionChanged="FileListBox_SelectionChanged"/>
            </GroupBox>

            <!-- 🖍️ Diff Viewer -->
            <GroupBox Header="File Diff" Grid.Column="1" Margin="5">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Old Version -->
                        <StackPanel Grid.Column="0" Margin="5">
                            <TextBlock Text="Before" FontWeight="Bold"/>
                            <ItemsControl x:Name="OldFileLines"/>
                        </StackPanel>

                        <!-- Separator -->
                        <Border Grid.Column="1" Background="Gray" Width="1"/>

                        <!-- New Version -->
                        <StackPanel Grid.Column="2" Margin="5">
                            <TextBlock Text="After" FontWeight="Bold"/>
                            <ItemsControl x:Name="NewFileLines"/>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>
            </GroupBox>
        </Grid>
    </Grid>
</Page>
